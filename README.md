# Global-Seismic-Trends-Earthquake-insights-
The project analyzes earthquake data extracted from USGS API  using python. EDA to understand seismic trends using magnitude, depth. The processed data was stored in MySQL, queried using SQL. SQLAlchemy was used to connect the database with streamlit application, to visualize insights, display query results, shows exploration of patterns
